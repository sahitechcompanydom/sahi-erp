# Supabase — copy this file to .env.local and fill in real values from Supabase Dashboard → Settings → API
# Restart the dev server (Ctrl+C then npm run dev) after saving .env.local
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Required for adding new personnel with temporary password (Step 16). Dashboard → Project Settings → API → service_role (secret).
# Never expose this key in the browser or client code.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# WhatsApp (Step 11) — optional; for onboarding and task notifications
# Use your gateway (e.g. UltraMsg, Twilio) endpoint and token
# WHATSAPP_API_URL=https://api.example.com/send
# WHATSAPP_TOKEN=your-token
# NEXT_PUBLIC_APP_URL=http://localhost:3001
