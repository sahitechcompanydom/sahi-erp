export const metadata = {
  title: "Task Tracker — Sahi Company",
  description: "Task and job tracking",
};

export default function TasksLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
